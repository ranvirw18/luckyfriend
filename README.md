Lucky Friend Spinner (Tkinter App)

This is a simple Python GUI application built using Tkinter.
It allows the user to:

Add friend names

Display the list

Randomly select a lucky friend using a spin button

Run the App
python luckyfriend_spin.py

Requirements

Python 3.x

Tkinter (comes pre-installed with most Python distributions)
